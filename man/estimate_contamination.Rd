% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pre-processing_soupX.R
\name{estimate_contamination}
\alias{estimate_contamination}
\title{Estimate and Remove Ambient RNA Contamination}
\usage{
estimate_contamination(
  soup_list,
  seurat_list,
  cts_filtered_list,
  sample_names,
  mulFac = 0,
  manual_contam = list(),
  manual = FALSE,
  methods = "subtraction",
  pCut = 0.01
)
}
\arguments{
\item{soup_list}{A named list of \code{SoupChannel} objects, typically generated by \code{create_soup_channels()}.}

\item{seurat_list}{A named list of clustered \code{Seurat} objects, typically generated by \code{create_seurat_for_clustering()}. Used solely to supply high-resolution cluster identities back to the SoupChannels.}

\item{cts_filtered_list}{A named list of filtered sparse count matrices. Used to extract the exact cell barcodes to ensure 1:1 mapping with the clusters.}

\item{sample_names}{Character vector. A list of sample identifiers corresponding to the names in the input lists.}

\item{mulFac}{Numeric. A percentage to artificially add to the auto-estimated contamination fraction (e.g., \code{mulFac = 5} adds \code{0.05} to the \code{rho} value). Default is 0.}

\item{manual_contam}{Character vector. A list of specific sample names to apply the \code{mulFac} adjustment to. Only evaluated if \code{manual = TRUE}. Default is an empty list.}

\item{manual}{Logical. Controls the application of \code{mulFac}. If \code{FALSE}, \code{mulFac} is applied globally to ALL samples. If \code{TRUE}, \code{mulFac} is applied ONLY to the specific samples listed in \code{manual_contam}. Default is FALSE.}

\item{methods}{Character. The method to use for count adjustment passed to \code{SoupX::adjustCounts()}. Options include "subtraction", "soupOnly", or "multinomial". Default is "subtraction".}

\item{pCut}{Numeric. The p-value threshold used for identifying cells expressing genes above background levels when \code{methods = "soupOnly"}. Default is 0.01.}
}
\value{
A named list containing two elements:
\itemize{
\item \code{soup_list_all}: A list of the updated \code{SoupChannel} objects with their final \code{rho} values calculated and set.
\item \code{corrected_list}: A list of the final, decontaminated sparse count matrices (rounded to integers), ready to be used to create your final Seurat objects.
}
}
\description{
Integrates \code{SoupChannel} objects with initial high-resolution clustering
to automatically estimate the ambient RNA contamination fraction (\code{rho})
via \code{SoupX::autoEstCont()}. Includes a robust manual override system
allowing you to artificially increase the estimated contamination fraction
(e.g., adding 5\% extra background removal) either globally across all samples
or selectively for specific noisy samples. Finally, it applies the chosen
correction method to remove the background noise and returns the cleaned count matrices.
}
\examples{
\dontrun{
# Assuming soup_channels and seurat_clusters were generated previously

# Example 1: Run standard auto-estimation with no manual overrides
clean_data <- estimate_contamination(
  soup_list = soup_channels,
  seurat_list = initial_seurat_objs,
  cts_filtered_list = counts_list$filtered,
  sample_names = my_samples,
  methods = "subtraction"
)

# Example 2: Sample "Patient_3" looks particularly noisy.
# Add an extra 5\% (0.05) to its estimated contamination fraction.
clean_data_adjusted <- estimate_contamination(
  soup_list = soup_channels,
  seurat_list = initial_seurat_objs,
  cts_filtered_list = counts_list$filtered,
  sample_names = my_samples,
  mulFac = 5,
  manual = TRUE,
  manual_contam = c("Patient_3")
)

# Extract the cleaned matrix for downstream processing
p3_clean_matrix <- clean_data_adjusted$corrected_list[["Patient_3"]]
}
}
