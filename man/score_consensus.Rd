% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Separating_CD8_NK.R
\name{score_consensus}
\alias{score_consensus}
\title{Calculate Consensus Labels for CD8 vs NK Cells}
\usage{
score_consensus(
  obj,
  cd8_cols,
  nk_col = "NK_Score",
  cells_use = colnames(obj),
  method = c("fixed", "trough", "gmm"),
  fixed_cut = 0,
  zscore = TRUE,
  primary = NULL,
  confirm = NULL,
  min_features = NULL,
  feature_col = "nFeature_RNA",
  n_boot = 0,
  boot_target = c("cutoff", "proportion"),
  add_meta = TRUE,
  prefix = "CD8vNK",
  seed = 42,
  make_plots = FALSE,
  plot_group_by = c("consensus", "variant_label"),
  plot_n = 1000,
  out_dir = NULL
)
}
\arguments{
\item{obj}{A single-cell object (e.g., Seurat) that contains a \verb{@meta.data} slot.}

\item{cd8_cols}{Character vector; names of the columns in \code{obj@meta.data} containing the CD8 scores to test.}

\item{nk_col}{Character string; name of the column containing the baseline NK score. Default: \code{"NK_Score"}.}

\item{cells_use}{Character vector; specific cell barcodes to include. Defaults to all cells in \code{obj}.}

\item{method}{Character; the method to determine the CD8 vs NK decision boundary. One of \code{"fixed"}, \code{"trough"} (density valley), or \code{"gmm"} (Gaussian Mixture Model).}

\item{fixed_cut}{Numeric; the threshold to use if \code{method = "fixed"}. Default: \code{0}.}

\item{zscore}{Logical; whether to standardize (Z-score) the CD8 and NK columns independently before subtracting them. Default: \code{TRUE}.}

\item{primary}{Character string; the CD8 variant to act as the primary label for the consensus calculation. Defaults to \code{cd8_cols[1]}.}

\item{confirm}{Character string; the CD8 variant to act as the confirming label for the consensus calculation. Defaults to \code{cd8_cols[2]}.}

\item{min_features}{Numeric; optional threshold to filter out low-quality cells prior to scoring.}

\item{feature_col}{Character string; the metadata column used for \code{min_features} filtering. Default: \code{"nFeature_RNA"}.}

\item{n_boot}{Numeric; number of bootstrap iterations to run to test cutoff/proportion stability. Set to \code{0} to skip.}

\item{boot_target}{Character; what metric to track during bootstrapping. Either \code{"cutoff"} (the threshold value) or \code{"proportion"} (fraction of cells labeled CD8).}

\item{add_meta}{Logical; whether to append the computed differences, scaled scores, and labels back into \code{obj@meta.data}.}

\item{prefix}{Character string; the prefix applied to the new columns added to \code{meta.data}. Default: \code{"CD8vNK"}.}

\item{seed}{Numeric; the random seed used for bootstrapping and plot downsampling. Default: \code{42}.}

\item{make_plots}{Logical; whether to generate diagnostic plots (Violin distributions, Agreement Heatmaps, etc.).}

\item{plot_group_by}{Character; how to group/color cells in the violin plots. Either \code{"consensus"} or by individual \code{"variant_label"}.}

\item{plot_n}{Numeric; maximum number of cells per class to sample for violin plots to ensure fast rendering. Default: \code{1000}.}

\item{out_dir}{Character string; optional directory path to save the generated plots (e.g., \code{"./plots/"}). If \code{NULL}, plots are only returned in the output list.}
}
\value{
A list containing:
\itemize{
\item \code{obj}: The updated single-cell object with new metadata columns (if \code{add_meta = TRUE}).
\item \code{per}: A list of data frames with detailed, per-cell scoring and labeling for each variant.
\item \code{labels}: A matrix of the assigned labels (CD8/NK) for all cells across all variants.
\item \code{consensus}: A named character vector of the final consensus labels per cell.
\item \code{agreement}: A pairwise matrix showing the proportion of label agreement between variants.
\item \code{n_disagree}: Integer; the number of cells that had conflicting labels among any variants.
\item \code{cutoffs}: A data frame summarizing the calculated thresholds and statistics for each variant.
\item \code{bootstrap}: A list containing bootstrap draws and summary statistics (if \code{n_boot > 0}).
\item \code{plots}: A list of \code{ggplot2} objects generated during the run (if \code{make_plots = TRUE}).
\item \code{cells_used}: Character vector of valid cell IDs retained after filtering.
\item \code{dropped}: Character vector of cell IDs dropped during filtering.
\item \code{params}: A list recording the input parameters used for this run.
}
}
\description{
Evaluates competing CD8 scoring variants against a baseline NK score to assign discrete
cell labels (CD8 vs NK). It establishes a cutoff threshold (using fixed, density trough,
or Gaussian Mixture Model methods), calculates consensus between a primary and confirmatory
variant, optionally bootstraps for stability, and generates diagnostic plots.
}
\examples{
\dontrun{
# Basic usage with a fixed cutoff of 0 (on Z-scored data)
res <- score_consensus(
  obj = seurat_obj,
  cd8_cols = c("CD8_Score_TCR", "CD8_Score_GEX"),
  nk_col = "NK_Score",
  method = "fixed",
  fixed_cut = 0,
  make_plots = TRUE,
  out_dir = "./scoring_plots"
)

# Extract the updated object for downstream Seurat workflows
seurat_obj <- res$obj
}
}
